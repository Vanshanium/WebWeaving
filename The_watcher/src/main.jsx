import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'
ReactDOM.createRoot(document.getElementById('root')).render(<App />);


// It just searches for the root in the html DOM and renders whatever is in <App>
// document is a special object which represent the entire html

